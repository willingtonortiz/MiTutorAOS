spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/MiTutorAOS
    username: postgres
    password: jair96
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    generate-ddl: true
    show-sql: true 